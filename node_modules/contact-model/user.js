import Transmission from 'contact-model/transmission'

class UserTransmission extends Transmission {
  type = 'u'

  isEmpty () {
    return !(typeof this.from.name === 'string' && this.from.name.length > 0)
  }
}

export default UserTransmission
